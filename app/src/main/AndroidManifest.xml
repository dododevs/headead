<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="revolver.headead">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.VIBRATE"/>

    <application
        android:name=".App"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.activities.MainActivity"/>
        <activity android:name=".ui.activities.DrugLookupActivity"/>
        <activity android:name=".ui.activities.record.RecordHeadacheActivity"/>
        <activity android:name=".ui.activities.record.RecordGeoActivity"/>
        <activity android:name=".ui.activities.record.RecordTriggersActivity"/>
        <activity android:name=".ui.activities.record.RecordDrugsActivity"/>

        <activity android:name=".ui.activities.record.RecordHeadacheActivity2">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.activities.record.RecordHeadacheActivity3"/>
        <activity android:name=".ui.activities.record.RecordHeadacheActivity4"/>

        <activity android:name=".ui.activities.DrugIntakeActivity"/>

        <meta-data android:name="com.google.android.geo.API_KEY"
            android:value=""/>
    </application>

</manifest>
