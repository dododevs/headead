<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:layout_marginHorizontal="16dp"
        android:layout_marginBottom="16dp"
        android:orientation="horizontal"
        android:background="@drawable/bg_mtrl_date_picker_header"
        android:elevation="1.8dp">

        <ImageView
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:layout_gravity="center_vertical"
            android:padding="4dp"
            android:focusable="true"
            android:clickable="true"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:id="@+id/mtrl_date_picker_year_prev"
            android:src="@drawable/ic_chevron_left"
            android:contentDescription="@string/cd_year_prev"
            app:tint="@color/blackOlive"/>

        <revolver.headead.ui.views.typography.LektonTextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_vertical"
            android:id="@+id/mtrl_date_picker_year"
            android:textColor="@color/blackOlive"
            android:textSize="13.8sp"
            app:textStyle="bold"/>

        <ImageView
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:layout_gravity="center_vertical"
            android:padding="4dp"
            android:focusable="true"
            android:clickable="true"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:id="@+id/mtrl_date_picker_year_next"
            android:src="@drawable/ic_chevron_right"
            android:contentDescription="@string/cd_year_next"
            app:tint="@color/blackOlive"/>

        <View
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"/>

        <ImageView
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:layout_gravity="center_vertical"
            android:padding="4dp"
            android:focusable="true"
            android:clickable="true"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:id="@+id/mtrl_date_picker_month_prev"
            android:src="@drawable/ic_chevron_left"
            android:contentDescription="@string/cd_month_prev"
            app:tint="@color/blackOlive"/>

        <revolver.headead.ui.views.typography.LektonTextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_vertical"
            android:id="@+id/mtrl_date_picker_month"
            android:textColor="@color/blackOlive"
            android:textSize="13.8sp"
            app:textStyle="bold"/>

        <ImageView
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:layout_gravity="center_vertical"
            android:padding="4dp"
            android:focusable="true"
            android:clickable="true"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:id="@+id/mtrl_date_picker_month_next"
            android:src="@drawable/ic_chevron_right"
            android:contentDescription="@string/cd_month_next"
            app:tint="@color/blackOlive"/>

    </LinearLayout>

    <androidx.recyclerview.widget.RecyclerView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:id="@+id/mtrl_date_picker_calendar"
        android:overScrollMode="never"
        android:scrollbars="none"/>

</LinearLayout>